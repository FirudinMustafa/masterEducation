/**
 * Auth route group — minimal layout. Storefront header/footer YOK; AuthShell
 * kendi 2-column layout'unu (sol brand panel + sağ form) full-screen kaplar.
 * Apple/Linear/Stripe tarzı odaklı login deneyimi.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
